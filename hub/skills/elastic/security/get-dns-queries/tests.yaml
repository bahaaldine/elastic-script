skill: get_dns_queries
category: security
tests:
- name: default_execution
  description: Test get_dns_queries with default/test parameters
  fixtures_required:
  - security-test
  expected_result_type: array
