name: install-esql-report-workflow
version: 1.0.0
description: Install a scheduled ES|QL reporting workflow
author: elastic
category: workflows
tags:
  - workflows
  - esql
  - reporting
  - scheduled
license: Elastic-2.0
requirements:
  elasticsearch: ">=8.0.0"
  kibana: ">=8.0.0"
  features:
    - workflows
main: skill.sql
