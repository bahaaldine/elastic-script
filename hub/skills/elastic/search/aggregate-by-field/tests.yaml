skill: aggregate_by_field
category: search
tests:
- name: default_execution
  description: Test aggregate_by_field with default/test parameters
  parameters:
    index_pattern: logs-test
    field: test-value
  fixtures_required:
  - content-test
  expected_result_type: array
