skill: get_mapping
category: search
tests:
- name: default_execution
  description: Test get_mapping with default/test parameters
  parameters:
    index_name: logs-test
  fixtures_required:
  - content-test
  expected_result_type: object
