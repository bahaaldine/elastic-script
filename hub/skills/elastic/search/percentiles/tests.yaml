skill: percentiles
category: search
tests:
- name: default_execution
  description: Test percentiles with default/test parameters
  parameters:
    index_pattern: logs-test
    field: test-value
  fixtures_required:
  - content-test
  expected_result_type: array
