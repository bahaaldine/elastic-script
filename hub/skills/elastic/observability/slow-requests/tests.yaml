skill: slow_requests
category: observability
tests:
- name: default_execution
  description: Test slow_requests with default/test parameters
  parameters:
    index_pattern: logs-test
  fixtures_required:
  - logs-test
  expected_result_type: array
